# RAG (Ollama + LangGraph)

Config via env vars:

- OLLAMA_BASE_URL (default http://localhost:11434)
- OLLAMA_EMBED_MODEL (default qwen3-embedding:8b)
- OLLAMA_LLM_MODEL (default qwen3:8b)
- RAG_TOP_K (default 5)
